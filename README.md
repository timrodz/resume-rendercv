# CV

## Requirements

Python 3.12

```bash
uv python install 3.12
uv venv --python 3.12
uv tool install "rendercv[full]"
```

## Development + rendering

```bash
rendercv render --watch Juan_Rodriguez_Morais_CV.yaml
```

Then just pay attention to the generated contents inside directory `rendercv_output`
